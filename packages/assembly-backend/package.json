{
  "name": "assembly-backend",
  "version": "0.1.1-preview",
  "private": true,
  "description": "Shared Node.js process manager and JSON-RPC client for .NET assembly backend tools",
  "main": "index.js",
  "types": "index.d.ts",
  "scripts": {
    "build": "node build.js",
    "build:all": "echo \"No platform builds for class library\"",
    "watch": "echo \"No watch mode for .NET packages\"",
    "test": "vitest run"
  }
}
