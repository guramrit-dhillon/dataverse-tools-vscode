// assembly-backend is a .NET class library — it gets built transitively
// by child packages (dataverse-assembly-analyzer, assembly-decompiler) via
// ProjectReference. No standalone build step needed.
console.log("[assembly-backend] Class library — built transitively by child packages.");
