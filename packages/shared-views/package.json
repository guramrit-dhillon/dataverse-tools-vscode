{
  "name": "shared-views",
  "version": "0.2.0-preview",
  "private": true,
  "main": "index.ts",
  "scripts": {
    "build": "echo 'Shared views — bundled by extension esbuild'",
    "package": "echo 'Shared views — bundled by extension esbuild'",
    "package:prerelease": "echo 'Shared views — bundled by extension esbuild'"
  }
}
