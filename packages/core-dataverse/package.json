{
  "name": "core-dataverse",
  "version": "0.2.1-preview",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "build": "echo 'Core library - built by extension esbuild'",
    "package": "echo 'Core library - packaged by extension esbuild'",
    "package:prerelease": "echo 'Core library - packaged by extension esbuild'"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "@vscode/codicons": "^0.0.44"
  }
}
